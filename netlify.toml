[build]
  command = "pnpm install --frozen-lockfile && pnpm --filter @indimba/web build"
  publish = "apps/web/.next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
