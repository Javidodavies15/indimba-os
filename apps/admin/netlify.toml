[build]
  command = "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter @indimba/admin build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
